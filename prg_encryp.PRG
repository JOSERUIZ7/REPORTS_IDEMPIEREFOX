PARAMETERS XSTRING, XTIPO

IF EMPTY(XTIPO)
	XTIPO = 'DENCRIPTAR'
	XSTRING = 'JOSE G RUIZ'
	XSTRING = '0000227D7F6171107E1465657B6E'
ENDIF

XSTRING = ALLTRIM(XSTRING)
XVAR = Createobject("DEVPOWERENCRYPT.ENCRYPT")

IF XTIPO = 'ENCRIPTAR'
	XCLAVEENCRIP=Alltrim(XVAR.EnCrypt(XSTRING,"7024094"))
ELSE

	XCLAVEENCRIP=Alltrim(XVAR.DECrypt(XSTRING,"7024094"))
ENDIF

RETURN XCLAVEENCRIP
